services:
  client:
    build:
      context: ./client
    ports:
      - "8080:80"
    container_name: ecommerce-client

  functions:
    build:
      context: ./functions
    container_name: ecommerce-functions